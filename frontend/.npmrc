# Fail `npm install`/`npm ci` outright on a Node below package.json engines (#52). Without this,
# npm only warns (EBADENGINE) and the real failure surfaces much later as an opaque ERR_REQUIRE_ESM
# from a transitive dependency, with no hint that the runtime version is the cause.
engine-strict=true
